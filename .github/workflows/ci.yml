name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      # 이미지 빌드 테스트
      - name: Build Docker image
        run: docker build -t fam_archive:ci .
